apiVersion: v1
kind: Service
metadata:
  name: system2-server-service
spec:
  type: ClusterIP # Internal access, external via port-forward
  selector:
    app: system2-server
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
